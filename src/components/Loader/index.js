import React from 'react';

const Loader = () => (
  <div
    style={{
      textAlign: 'center',
      height: '100vh',
      width: '100%',
    }}
  >
    <p
      style={{
        marginTop: '50%',
        fontFamily: "'Reenie Beanie', cursive",
        color: '#fff',
      }}
    >Loading</p>
  </div>
)

export default Loader
