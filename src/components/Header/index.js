import React from 'react';

const Header = () => (
  <div style={{ textAlign: 'center' }}>
    <img
      style={{
        marginTop: '15px',
        width: '100%',
        maxWidth: '900px',
      }}
      src="/static/logo.png" alt=""/>
  </div>
)

export default Header
